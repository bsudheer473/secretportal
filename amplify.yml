version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd packages/frontend
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: packages/frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - packages/frontend/node_modules/**/*
